const setupEventListeners = () => {};

const initStore = () => {
  // pole parenti storet, kuhu külge slice panna
};

const initModule = () => {
  console.log('SIIN initin');

  setupEventListeners();
  initStore();
};

initModule();
